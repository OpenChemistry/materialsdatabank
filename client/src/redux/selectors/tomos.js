// Pure state selection
export const getSearchResults = state => state.tomos.search.results;

export const getSearchError = state => state.tomos.search.error;

export const getTomosById = state => state.tomos.byId;
